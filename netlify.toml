[build]
  functions = "netlify/functions"
  publish = "."
  command = "echo 'No build command'"
  ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF . ../README.md ../task.md ../implementation_plan.md ../walkthrough.md"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
