[build]
  functions = "netlify/functions"
  publish = "."      # Usa la root per servire checkin.html e registrazione/
  command = "exit 0"      # Dice a Netlify: "Esci subito, non fare nulla"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200